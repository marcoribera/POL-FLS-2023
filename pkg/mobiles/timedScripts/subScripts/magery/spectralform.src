 
use uo;
use os;

include ":attributes:attributes";

program TimedScript_Clumsy(params)
	var who := params[1];
	var attacker := params[2];
	var level := params[3];
	params := 0; // Not needed anymore.

	params := 0; // Not needed anymore.

	SendSysMessage(mobile, "Voce assume uma forma espectral.");
	//printtextabove(mobile, " " + mobile.paralyzed);
	SetObjProperty(mobile, "#spectralform", mobile.color);
	mobile.color := 30000;
	
	return 1;
endprogram

   
